# Architecture Decision Records

Decisions that shape how this codebase is built. Read the relevant ADR before changing anything it governs; supersede with a new ADR rather than editing history.

- [ADR-0001: Consolidate business logic into deep domain modules](0001-domain-modules.md) — accepted. Governs `src/modules/`, the ESLint import boundary, and the deliberate two-photo-tables split.
