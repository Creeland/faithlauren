// Test stub for the `server-only` package. The published module throws on
// import outside a React Server Component; under Vitest it is aliased here to a
// harmless empty module (see vitest.config.ts) so server-only domain code can
// be imported and exercised directly.
export {};
